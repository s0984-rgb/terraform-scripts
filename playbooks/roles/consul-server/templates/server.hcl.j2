# TLS
verify_incoming = true

addresses {
  http    = "0.0.0.0"
  https    = "0.0.0.0"
  grpc_tls = "0.0.0.0"
}

advertise_addr   = "{{ ansible_default_ipv4.address }}"
server           = true
bootstrap_expect = 1

ui_config {
  enabled = true
}